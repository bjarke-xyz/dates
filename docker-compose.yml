version: "3.7"
services:
  dates:
    image: registry.bjarke.xyz/dates
    build:
      context: ./
    networks:
      - swarm-overlay

networks:
  swarm-overlay:
    external: true
    name: swarm-overlay
